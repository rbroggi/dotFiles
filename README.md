
My Ubuntu setup step by step:

1. first packages:

```bash
$ sudo apt-get install vim python git
$ git config --global user.email <email>
$ git config --global user.name <user> 
```
2. clone dotfiles:
```bash
$ cd 
$ git clone <repo_url>
```
3. place the dotfiles in the home directory
```bash

```
